version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: "pet-shop"
      POSTGRES_USER: "rusik"
      POSTGRES_PASSWORD: "12345"
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - 5432:5432

